<style>
.btn.btn-success {
    height: 40px;
    width: 120px;
}
</style>

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-success">{{ button_label|default('Envoyer') }}</button>
{{ form_end(form) }}
